//string problem generate hoto  dhiraj manik takate asa taklyavar fakt dhiraj read hot so this is the problem

#include<stdio.h>

int main()
{
    char arr[20];   //static memory allocation

    printf("Enter the string : \n");
    scanf("%s",arr);
    
    printf("entered string is : %s\n",arr);
    
    return 0; 
}

//string enter kartana aapn jar space dili tar tya space chya pudhcha kahich scan hot nahi space n deta input dila tar barobar scan hot ha problem yeto %s ney string scan kelyavar so updation needed